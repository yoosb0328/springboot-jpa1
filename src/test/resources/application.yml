## test는 test/resource 가 우선권을 갖는다.
# SpringBoot는 별도의 설정이 없으면 메모리모드로 실행한다.
#spring:
#  datasource:
#    url: jdbc:h2:mem:test #메모리 모드 설정
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#  jpa:
#    hibernate:
#      ddl-auto: create-drop
#    properties:
#      hibernate:
#        format_sql: true


logging.level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace #쿼라 파라미터 로그 남기기.